class Score {
    constructor(x, y, color, font) {
        this.x = x;
        this.y = y;
        this.color = color
        this.font = font
        this.score = score
    }

    update() {
        this.score += 1;
    }

    reset() {
        this.points = 0;
    }

    // unsure how to update ctx

    }